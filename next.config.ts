import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
        domains: ['pro.lviv.ua', "th.bing.com", "svitkavy.com", "tse3.mm.bing.net"],
    },
};

export default nextConfig;
